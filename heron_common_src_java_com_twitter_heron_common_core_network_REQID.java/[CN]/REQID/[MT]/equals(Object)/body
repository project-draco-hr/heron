{
  if (other == null)   return false;
  if (other == this)   return true;
  if (!(other instanceof REQID))   return false;
  REQID rother=(REQID)(other);
  return Arrays.equals(this.bytes,rother.value());
}
