{
  this.config=config;
  this.runtime=runtime;
  this.controller=new AuroraController(Runtime.topologyName(runtime),Context.cluster(config),Context.role(config),Context.environ(config),Context.verbose(config));
}
