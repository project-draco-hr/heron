{
  Assert.assertEquals(Defaults.executorBinary(),Context.executorBinary(props));
  Assert.assertEquals(Defaults.stmgrBinary(),Context.stmgrBinary(props));
  Assert.assertEquals(Defaults.tmasterBinary(),Context.tmasterBinary(props));
  Assert.assertEquals(Defaults.shellBinary(),Context.shellBinary(props));
}
