{
  home=ClusterDefaults.getDefaultHome();
  sandbox=ClusterDefaults.getSandboxHome();
  props=Config.newBuilder().putAll(ClusterDefaults.getDefaultJars()).putAll(ClusterDefaults.getSandboxBinaries()).build();
}
