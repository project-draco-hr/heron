{
  LOG.info("Persist FrameworkId: " + frameworkID);
  try {
    Files.write(new File(frameworkIdPath).toPath(),frameworkID.getValue().getBytes());
  }
 catch (  IOException e) {
    LOG.log(Level.SEVERE,"Failed to write file",e);
    return false;
  }
  return true;
}
