@tornado.gen.coroutine
def get(self):
    clusters = yield access.get_clusters()
    options = dict(topologies=[], clusters=map(str, clusters), active='topologies', function=common.className)
    self.render('topologies.html', **options)
