@tornado.gen.coroutine
def get(self):
    clusters = yield access.get_clusters()
    clusters = [str(c['name']) for c in clusters['clusters']]
    options = dict(topologies=[], clusters=clusters, active='topologies', function=common.className)
    self.render('topologies.html', **options)
