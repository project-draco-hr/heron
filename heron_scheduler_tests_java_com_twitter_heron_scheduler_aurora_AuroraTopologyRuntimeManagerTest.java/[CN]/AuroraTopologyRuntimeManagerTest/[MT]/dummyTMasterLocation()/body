{
  return TopologyMaster.TMasterLocation.newBuilder().setHost(tmasterHost).setMasterPort(0).setTopologyId(topologyName).setTopologyName(topologyName).setControllerPort(tmasterControlPort).build();
}
