{
  SettableFuture<Boolean> future=SettableFuture.create();
  boolean ret=FileUtils.writeToFile(getExecutionStatePath(topologyName),executionState.toByteArray());
  future.set(ret);
  return future;
}
