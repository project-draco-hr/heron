def cygpath(x_val):
    '\n  :param x_val:\n  :return:\n  '
    command = ['cygpath', '-wp', x_val]
    proc = subprocess.Popen(command, stdout=subprocess.PIPE)
    (output, _) = proc.communicate()
    lines = output.split('\n')
    return lines[0]
