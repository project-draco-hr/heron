def __init__(self):
    socket_options = SocketOptions(32768, 16, 32768, 16, 1024000, 1024000)
    sys_config = {constants.INSTANCE_RECONNECT_METRICSMGR_INTERVAL_SEC: 10, }
    MetricsManagerClient.__init__(self, EventLooper(), self.HOST, self.PORT, mock_protobuf.get_mock_instance(), HeronCommunicator(), {}, socket_options, sys_config)
    self.register_req_called = False
