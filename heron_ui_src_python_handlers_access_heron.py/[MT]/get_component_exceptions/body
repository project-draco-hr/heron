@tornado.gen.coroutine
def get_component_exceptions(cluster, environ, topology, component):
    request_url = tornado.httputil.url_concat(create_url(EXCEPTIONS_URL_FMT), dict(cluster=cluster, environ=environ, topology=topology, component=component))
    raise tornado.gen.Return(yield fetch_url_as_json(request_url))
