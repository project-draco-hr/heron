@tornado.gen.coroutine
def get_instance_mem_histogram(cluster, environ, topology, instance):
    request_url = tornado.httputil.url_concat(create_url(HISTOGRAM_URL_FMT), dict(cluster=cluster, environ=environ, topology=topology, instance=instance))
    raise tornado.gen.Return(yield fetch_url_as_json(request_url))
