{
  if (!hasStream()) {
    return false;
  }
  if (!hasGtype()) {
    return false;
  }
  if (!getStream().isInitialized()) {
    return false;
  }
  if (hasGroupingFields()) {
    if (!getGroupingFields().isInitialized()) {
      return false;
    }
  }
  return true;
}
