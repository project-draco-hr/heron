{
  if (!hasStream()) {
    return false;
  }
  if (!hasSchema()) {
    return false;
  }
  if (!getStream().isInitialized()) {
    return false;
  }
  if (!getSchema().isInitialized()) {
    return false;
  }
  return true;
}
