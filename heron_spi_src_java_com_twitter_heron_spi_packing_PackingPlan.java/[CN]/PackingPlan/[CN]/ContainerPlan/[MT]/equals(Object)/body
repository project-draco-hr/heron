{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ContainerPlan that=(ContainerPlan)o;
  return id.equals(that.id) && getInstances().equals(that.getInstances()) && getResource().equals(that.getResource());
}
