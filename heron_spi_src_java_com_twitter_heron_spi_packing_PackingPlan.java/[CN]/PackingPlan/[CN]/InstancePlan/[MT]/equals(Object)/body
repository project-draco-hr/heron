{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  InstancePlan that=(InstancePlan)o;
  return getId().equals(that.getId()) && getComponentName().equals(that.getComponentName()) && getResource().equals(that.getResource());
}
