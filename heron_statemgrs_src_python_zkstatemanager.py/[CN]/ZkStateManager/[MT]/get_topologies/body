def get_topologies(self, callback=None):
    isWatching = False
    ret = {'result': None, }
    if callback:
        isWatching = True
    else:

        def callback(data):
            ret['result'] = data
    self._get_topologies_with_watch(callback, isWatching)
    return ret['result']
