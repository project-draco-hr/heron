def topology_jars():
    jars = [pick(utils.get_heron_lib_dir(), 'protobuf-java-*.jar'), pick(utils.get_heron_lib_dir(), 'log4j-over-slf4j-*.jar'), pick(utils.get_heron_lib_dir(), 'slf4j-api-*.jar'), pick(utils.get_heron_lib_dir(), 'slf4j-jdk*.jar')]
    return jars
