{
  assertNotAlive();
  this.topologyName=topologyName;
  this.conf=conf;
  this.topology=topology;
  localMode.submitTopology(topologyName,ConfigUtils.translateConfig(conf),topology.getStormTopology());
}
