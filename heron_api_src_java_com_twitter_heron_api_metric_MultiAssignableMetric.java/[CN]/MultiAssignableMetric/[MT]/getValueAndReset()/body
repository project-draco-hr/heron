{
  Map ret=new HashMap();
synchronized (value) {
    for (    Map.Entry<String,AssignableMetric> e : value.entrySet()) {
      ret.put(e.getKey(),e.getValue().getValueAndReset());
    }
  }
  return ret;
}
