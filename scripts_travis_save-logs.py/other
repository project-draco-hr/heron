import mmap
import os
import subprocess
import sys
from datetime import datetime, timedelta
if (__name__ == '__main__'):
    if (sys.argv < 1):
        print ('Usage: %s [file info]' % sys.argv[0])
        sys.exit(1)
    file = sys.argv[1]
    cmd = sys.argv[2:]
    main(file, cmd)
