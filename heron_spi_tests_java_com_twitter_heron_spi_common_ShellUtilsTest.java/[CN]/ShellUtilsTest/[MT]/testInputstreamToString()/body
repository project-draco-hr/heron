{
  String testString=generateRandomLongString(1024 * 1024);
  ByteArrayInputStream is=new ByteArrayInputStream(testString.getBytes());
  Assert.assertEquals(testString,ShellUtils.inputstreamToString(is));
}
