{
  Config props=ClusterConfig.loadSchedulerConfig(basicConfig);
  Assert.assertEquals(2,props.size());
  Assert.assertEquals("com.twitter.heron.scheduler.local.LocalLauncher",Context.launcherClass(props));
  Assert.assertEquals("com.twitter.heron.scheduler.local.LocalRuntimeManager",Context.runtimeManagerClass(props));
}
